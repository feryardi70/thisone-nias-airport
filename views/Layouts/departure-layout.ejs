<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DEPARTURE</title>

    <!-- //bootstrap stylesheet -->
    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" /> -->
    <link href="css/bootstrap.min.css" rel="stylesheet" />

    <style>
      html,
      body {
        height: 100%;
        margin: 0;
      }
      body {
        font-size: 24px;
        color: #f5f9fa; /* Warna teks */
        background-color: #0000cd; /* Warna latar belakang */
      }
      table {
        width: 100%;
        /* border-collapse: collapse; */
      }
      thead {
        background-color: #130704;
        font-size: 30px;
      }
      th {
        border-bottom: 2px solid #a01919;
        padding: 8px;
        text-align: center;
      }
      td {
        border-bottom: 1px solid #ddd;
        padding: 8px;
        text-align: center;
      }
      .noP {
        display: inline;
      }
      .container-depart {
        display: flex;
        justify-content: center;
        /* align-items: center; */
        padding: 0px;
        margin-top: 50px;
      }

      .depart {
        width: 100%;
        background-color: #0c0401; /* Warna latar belakang contoh */
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }

      .bottom-center {
        margin: 0px 0;
        font-size: 45px;
        letter-spacing: 25px;
      }
      .date {
        margin: 0px 0;
        font-size: 18px;
        letter-spacing: 4px;
      }
      .pukul {
        margin: 0px 0;
        font-size: 32px;
        letter-spacing: 4px;
      }
      /* th {
        background-color: #0000cd;
      } */
      /* tr:nth-child(even) {
        background-color: #f2f2f2;
      } */
    </style>
  </head>
  <body>
    <%- body %>
  </body>
</html>
